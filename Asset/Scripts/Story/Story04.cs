using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Story04 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        SettingsManager.UnlockLevel("3");
    }

    // Update is called once per frame
    void Update()
    {

    }
}
